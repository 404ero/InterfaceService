server:
   port: 8888

logging:
  config: classpath:logback.xml


spring:
   application:
          name: userManager
   datasource:
       driver-class-name: com.mysql.jdbc.Driver
       url: jdbc:mysql://localhost:3306/test
       username: root
       password: 922280

mybatis:
    type-aliases-package: com.comein.model
    mapper-locations:
       - mapper/*
